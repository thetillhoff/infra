apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: trading-operator
  namespace: trading
spec:
  imageRepositoryRef:
    name: trading-operator
  filterTags:
    pattern: '^(?P<run>[0-9]+)$'
    extract: '$run'
  policy:
    numerical:
      order: asc
