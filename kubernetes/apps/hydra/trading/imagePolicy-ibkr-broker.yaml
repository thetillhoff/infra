apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: trading-ibkr-broker
  namespace: trading
spec:
  imageRepositoryRef:
    name: trading-ibkr-broker
  filterTags:
    pattern: '^(?P<run>[0-9]+)$'
    extract: '$run'
  policy:
    numerical:
      order: asc
