apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui
  namespace: manga-scraper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ui
  template:
    metadata:
      labels:
        app: ui
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      containers:
        - name: ui
          image: ghcr.io/thetillhoff/manga-scraper-ui:48 # {"$imagepolicy": "manga-scraper:manga-scraper-ui"}
          ports:
            - containerPort: 3000
          env:
            - name: API_BASE_URL
              value: http://api:3001
            - name: WEB_PORT
              value: "3000"
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          # /health answers from the ui process alone. Probing a real page would
          # make this pod's life depend on api and postgres, which have health
          # checks of their own — a slow catalog query then restarts a ui that is
          # perfectly alive.
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 192Mi
