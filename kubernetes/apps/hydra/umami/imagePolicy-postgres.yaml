apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: postgres
spec:
  imageRepositoryRef:
    name: postgres
  filterTags:
    pattern: '^(?P<version>.*)'
    extract: '$version'
  policy:
    semver:
      range: '>=17.0'
