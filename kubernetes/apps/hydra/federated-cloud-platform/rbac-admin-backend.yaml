# admin-backend serves POST /scraper/trigger, which creates a Job from the
# scraper CronJob through the in-cluster API. The public backend used to hold
# these rights; it no longer touches the Kubernetes API at all.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: admin-backend
  namespace: federated-cloud-platform
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: admin-backend
  namespace: federated-cloud-platform
rules:
  - apiGroups: ["batch"]
    resources: ["cronjobs"]
    verbs: ["get"]
  - apiGroups: ["batch"]
    resources: ["jobs"]
    verbs: ["create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: admin-backend
  namespace: federated-cloud-platform
subjects:
  - kind: ServiceAccount
    name: admin-backend
    namespace: federated-cloud-platform
roleRef:
  kind: Role
  name: admin-backend
  apiGroup: rbac.authorization.k8s.io
