apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: fcp-public-web
  namespace: federated-cloud-platform
spec:
  imageRepositoryRef:
    name: fcp-public-web
  filterTags:
    pattern: '^(?P<run>[0-9]+)$'
    extract: '$run'
  policy:
    numerical:
      order: asc
