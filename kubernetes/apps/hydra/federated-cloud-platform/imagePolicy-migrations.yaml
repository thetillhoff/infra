apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: fcp-migrations
  namespace: federated-cloud-platform
spec:
  imageRepositoryRef:
    name: fcp-migrations
  filterTags:
    pattern: '^(?P<run>[0-9]+)$'
    extract: '$run'
  policy:
    numerical:
      order: asc
