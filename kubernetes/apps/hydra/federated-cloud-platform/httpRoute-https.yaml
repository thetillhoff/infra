apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: cloud-thetillhoff-de-https
spec:
  parentRefs:
    - name: https-gateway-thetillhoff-de
      namespace: gateways
  hostnames:
    - cloud.thetillhoff.de
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        # Public marketplace only. The public web app's Caddy proxies the public API
        # prefixes to public-api in-cluster; admin prefixes live only in the
        # admin-web Caddyfile, which is not routed here.
        - name: public-web
          port: 8080
