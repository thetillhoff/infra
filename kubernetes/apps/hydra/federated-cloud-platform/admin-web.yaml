apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-web
  namespace: federated-cloud-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-web
  template:
    metadata:
      labels:
        app: admin-web
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      containers:
        - name: admin-web
          image: ghcr.io/thetillhoff/federated-cloud-platform/admin-web:39 # {"$imagepolicy": "federated-cloud-platform:fcp-admin-web"}
          ports:
            - containerPort: 8080
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: admin-web
  namespace: federated-cloud-platform
spec:
  selector:
    app: admin-web
  ports:
    - port: 8080
      targetPort: 8080
