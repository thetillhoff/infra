apiVersion: v1
kind: Secret
metadata:
  name: fcp-clerk
  namespace: federated-cloud-platform
stringData:
  secretKey: CHANGEME        # Clerk secret key (sk_live_...)
  adminSubjects: CHANGEME    # Comma-separated Clerk user_* IDs for admin tier
