apiVersion: v1
kind: Secret
metadata:
  name: fcp-clerk
  namespace: federated-cloud-platform
stringData:
  secretKey: CHANGEME        # Clerk secret key (sk_live_...)
  # Admin tier is now the Clerk `role` session claim (publicMetadata.role ==
  # "admin"), configured in the Clerk dashboard - no allowlist secret needed.
