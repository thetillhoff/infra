apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: webscan
spec:
  imageRepositoryRef:
    name: webscan
  filterTags:
    pattern: '^(?P<version>.*)'
    extract: '$version'
  policy:
    semver:
      range: '>=5.0.0'
