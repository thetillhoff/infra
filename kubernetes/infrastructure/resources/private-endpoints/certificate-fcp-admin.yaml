apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: fcp-admin-internal
  namespace: private-endpoints
spec:
  secretName: fcp-admin-internal-tls
  dnsNames:
    - fcp-admin.internal.thetillhoff.de
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
