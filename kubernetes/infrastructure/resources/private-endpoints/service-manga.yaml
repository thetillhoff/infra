apiVersion: v1
kind: Service
metadata:
  name: manga-internal
  namespace: private-endpoints
  annotations:
    tailscale.com/hostname: manga-internal
    tailscale.com/tags: tag:service
    external-dns.alpha.kubernetes.io/hostname: manga.internal.thetillhoff.de
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app: manga-proxy
  ports:
    - name: https
      port: 443
      targetPort: 8443
