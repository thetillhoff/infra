apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: trading-internal
  namespace: private-endpoints
spec:
  secretName: trading-internal-tls
  dnsNames:
    - trading.internal.thetillhoff.de
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
