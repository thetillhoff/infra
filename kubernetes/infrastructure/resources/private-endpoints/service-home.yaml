apiVersion: v1
kind: Service
metadata:
  name: home-internal
  namespace: private-endpoints
  annotations:
    tailscale.com/hostname: home-internal
    tailscale.com/tags: tag:service
    external-dns.alpha.kubernetes.io/hostname: home.internal.thetillhoff.de
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app: home-proxy
  ports:
    - name: https
      port: 443
      targetPort: 8443
