apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: manga-internal
  namespace: private-endpoints
spec:
  secretName: manga-internal-tls
  dnsNames:
    - manga.internal.thetillhoff.de
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
