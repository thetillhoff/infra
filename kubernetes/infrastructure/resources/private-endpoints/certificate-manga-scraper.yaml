apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: manga-scraper-internal
  namespace: private-endpoints
spec:
  secretName: manga-scraper-internal-tls
  dnsNames:
    - manga-scraper.internal.thetillhoff.de
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
