name: Test

on:
  workflow_dispatch:

jobs:

  validate:
    runs-on: ubuntu-22.04
    steps:
      - run: |
          python -v
      - run: |
          python -m pip -v
      - run: |
          pip -V
