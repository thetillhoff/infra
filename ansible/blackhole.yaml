- name: Blackhole configuration
  hosts: blackhole
  connection: local

  # Requirements:
  # - python installed -> `sudo apt-get install -y python3`
  # - passwordless sudo

  roles:
    - basic
    - fileserver-blackhole
    - k3s
    - apps
    - disk-spindown
