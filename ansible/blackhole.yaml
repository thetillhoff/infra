- name: Blackhole configuration
  hosts: blackhole

  # Requirements:
  # - python installed -> `sudo apt-get install -y python3`
  # - passwordless sudo

  roles:
    - basic
    - tailscale
    #- zfs
    - k3s-ingress-nginx
    # - k3s-cert-manager
    - k3s-rook
    - k3s
    # - apps
