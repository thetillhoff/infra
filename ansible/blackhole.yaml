- name: Blackhole configuration
  hosts: blackhole

  # Requirements:
  # - python installed -> `sudo apt-get install -y python3`
  # - passwordless sudo

  roles:
    - basic
    - zfs
    - k3s-ingress-nginx
    - k3s
    - apps
    - disk-spindown
