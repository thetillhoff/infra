[defaults]
# Auto-decrypt sops-encrypted host_vars/group_vars (needs SOPS_AGE_KEY_FILE + sops).
vars_plugins_enabled = host_group_vars,community.sops.sops

[ssh_connection]
ssh_args = -o ServerAliveInterval=30
